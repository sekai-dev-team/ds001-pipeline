# Spec: Hermes Agent 源 + 削减 arXiv/HN 体积 (v0.5.2)

**状态:** 待实现  
**日期:** 2026-06-03

---

## 改动清单

### 1. 新增：Hermes Agent Releases（`pipeline/sources.yaml`）

在 RSS 源区域追加：

```yaml
  - name: "Hermes Agent Releases"
    url: "https://github.com/NousResearch/hermes-agent/releases.atom"
    tag: "source/hermes-agent"
    window: 72
```

标准 GitHub Releases Atom feed，与现有 LangChain Releases 同模式。`window: 72` 因为 release 发布频率低（几天一次），避免漏掉。

### 2. 削减：HN frontpage（`pipeline/sources.yaml` 第 48 行）

```
-      - "https://hnrss.org/frontpage?count=30"
+      - "https://hnrss.org/frontpage?count=20"
```

### 3. 削减：arXiv max_results（`pipeline/sources.py` 第 297 行）

```python
# line 297
-    "&sortBy=submittedDate&sortOrder=descending&max_results=50"
+    "&sortBy=submittedDate&sortOrder=descending&max_results=20"
```

---

## 验证

```bash
# Build & push
cd /tmp/ds001-pipeline
git add -A
git commit -m "feat: add Hermes Agent Releases source; reduce arXiv(50→20) HN(30→20)"
git push
./build-and-push.sh
```
